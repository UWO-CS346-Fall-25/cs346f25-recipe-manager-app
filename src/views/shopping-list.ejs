<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Re-Easy - <%= title %></title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <div id="content">
            <header>
                <nav>
                    <ul>
                        <li><a href="/">Home</a></li>
                        <li><a href="/about">About</a></li>
                        <li><a href="/my-recipes">My Recipes</a></li>
                        <li><a id="current-page">Shopping List</a></li>
                    </ul>
                </nav>
            </header>
            <main>
                <div id="tagline">
                    <h1>Re-Easy!</h1>
                    <p>Where Managing Recipes is Easy!</p>
                </div>

                <div id="shopping-list-container">
                    <section id="shopping-list">
                        
                        <ul id="shopping-list-items">

                            <!-- A list item for each ingredient in user's shopping list -->
                            <% if (items.length > 0) {%>

                                <% items.forEach(item => { %>
                                    <li class="ingredient">
                                        <input type="checkbox">
                                        <%= item %>
                                    </li>
                                <% }) %> 

                            <% } %>
                            
                            <li id="new-ingredient-container">
                                <form action="/addIngredient" method="post">
                                    <div id="new-ingredient-quanity">
                                        <label id="new-ingredient-label" for="new-ingredient">New Ingredient:</label>
                                        <textarea id="new-ingredient" placeholder="New Ingredient" name="newIngredient" required></textarea>

                                        <label for="count" id="count-label">Quantity:</label>
                                        <input type="number" id="count" name="count" min="1" max="10" step="1" required>
                                    </div>

                                    <button id="add-ingredient-button" type="submit">Add Item</button>
                                </form>
                            </li>
                            <li>
                                <form action="/deleteShoppingList" method="post">
                                    <button id="clear-list-button" type="submit">Delete List</button>
                                </form>
                            </li>
                        </ul>
                    </section>
                </div>
                

            </main>
            <footer>
                <p>
                    Note: This website is being developed for a class project and in no way should be
                    considered representative of any organization.
                </p>
                <div id="footer-buttons">
                    <button id="quote-button">
                        Daily Quote
                    </button>
                    <button id="log-out-button" title="Log out">
                        Log Out
                    </button>
                </div>
            </footer>
        </div>
        <script src="./js/shoppingListScript.js" defer></script>
        <script src="/js/quoteScript.js" type="module" defer></script>
    </body>
</html>